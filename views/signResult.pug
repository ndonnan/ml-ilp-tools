extends layout

block content
    div.row
        div.col
            h2 Signed ILP Packet
            h5 Condition
            p Provided in the quote response. This is a hash of the fulfilment known only to the payee FSP.
            pre= condition
            h5 Fulfilment
            p Provided by the payee FSP after a successful committed transaction and validated by the switch.
            pre= fulfilment
            h5 Packet
            pre= JSON.stringify(packet, undefined, 2)
            h5 Packet Data
            p This is the base64 decoded data from the packet above.
            pre= JSON.stringify(packetData, undefined, 2)
            button.btn.btn-primary(onclick='window.history.back()') Back
